import React from "react"

export default function EventDetail(params: { params: { slug: string } }) {

    // TODO:  Create function to fetch event by slug

    return (
        <div>
            
        </div>
    )
};

